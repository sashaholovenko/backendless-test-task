import React from 'react';

const Header = () => {
    return (
        <div style={{width: "100%", height: 100, background: "blue", color: "white", fontSize: 20, display: "flex", justifyContent: "center", alignItems: "center"}}>
            Test task for Backendless <br/>
            done by Oleksandr Holovenko
        </div>
    );
};

export default Header;